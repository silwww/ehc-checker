# Anthropic API key (get from https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Shared team password for accessing EHC Checker (single value, all 4 OVs use it)
# To rotate: change this value and redeploy. NOTE: existing sessions SURVIVE this —
# the session cookie is signed with EHC_COOKIE_SECRET, not with this password, so
# changing it only stops NEW logins with the old password. To sign everyone out
# (a lost or stolen device), rotate EHC_COOKIE_SECRET instead.
EHC_SHARED_SECRET=your-shared-team-password-here

# Server-side secret used to sign session cookies (any random 32+ char string)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
EHC_COOKIE_SECRET=your-random-cookie-signing-secret-here

# Model to use. Default in code is claude-sonnet-5. Leave this UNSET to use the
# code default; set it only to override (e.g. claude-sonnet-4-6 as a rollback lever).
CLAUDE_MODEL=claude-sonnet-5
